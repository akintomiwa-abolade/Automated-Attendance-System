const path = require('path');

module.exports = {
  "config": path.resolve('./middlewares/config', 'config.json'),
  "models-path": path.resolve('./middlewares/models'),
  "seeders-path": path.resolve('./middlewares/seeders'),
  "migrations-path": path.resolve('./middlewares/migrations')
};